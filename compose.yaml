services:
    postgres:
        image: "postgres:alpine3.20"
        environment:
            - "POSTGRES_DB=whattocook"
            - "POSTGRES_PASSWORD=password"
            - "POSTGRES_USER=thomas"
        ports:
            - "5433:5432"
